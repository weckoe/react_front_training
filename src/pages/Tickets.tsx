import React, {FC} from 'react';
import TicketListComponent from "../components/TicketsList";

const Tickets: FC = () => {
    return (
        <div>
            <TicketListComponent/>
        </div>
    );
};

export default Tickets;